version: '3.8'
services:
  flask-app:
    networks:
      - production-network

  node-addin:
    networks:
      - production-network

networks:
  production-network:
    external: true
    name: production-network  # Explicitly set the network name
