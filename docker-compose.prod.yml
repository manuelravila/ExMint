version: '3.8'
services:
  flask-app:
    networks:
      production-network:
        ipv4_address: 172.31.0.3

  node-addin:
    networks:
      production-network:
        ipv4_address: 172.31.0.4

networks:
  production-network:
    external: true